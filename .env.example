# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DATABASE=nhk_easy

# Optional HTTP proxy for both browser and httpx (e.g. http://127.0.0.1:7890)
HTTP_PROXY_URL=

# Directories (defaults are relative to the repo root)
# PROFILE_DIR=.chromium
# DATA_DIR=data
